# OpenSofa Environment Variables

# Optional: Custom config directory (defaults to ~/.opensofa)
# OPENSOFA_CONFIG_DIR=~/.opensofa

# Optional: Log level (debug, info, warn, error)
# LOG_LEVEL=debug

# Optional: Node environment
# NODE_ENV=development